"""
constants.py — re-exports config-loaded constants.

Nothing is hardcoded here. Values come from Settings loaded once at startup.
Import the Settings object (injected) rather than module-level constants.
"""
